# 補完用のファイルの読み込み
[[ -d "$HOME/.zsh/zsh-completions/src/" ]] && fpath=($HOME/.zsh/zsh-completions/src $fpath)

# filter用の関数の読み込み
[[ -s "$HOME/.functions/filter_function.sh" ]] && source $HOME/.functions/filter_function.sh
